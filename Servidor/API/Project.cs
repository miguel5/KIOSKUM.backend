using System;
using System.Collections.Generic;
using System.IO;
using System.Drawing;

namespace API
{
    public class Project
    {
        /*public static void Main(string[] args)
        {
            string Nome = "Arroz de Atum";
            string Categoria = "Pratos";
            double Preco = 4.05;
            IList<string> Ingredientes = new List<string>
            {
                "Arroz carolino",
                "Atum"
            };
            IList<string> Alergeneos = new List<string>
            {
                "Sementes",
                "Glúten"
            };
            Bitmap Fotografia = new Bitmap("/Users/lazaropinheiro/Downloads/arroz.jpg", true);

            
            Produto p1 = new Produto(Nome, Categoria, Preco, Ingredientes, Alergeneos, Fotografia);
            //Console.WriteLine(p1.ToString());

            Nome = "Massa com Atum";
            Ingredientes.Clear();
            Ingredientes.Add("Massa");
            Ingredientes.Add("Atum");
            Produto p2 = new Produto(Nome, Categoria, Preco, Ingredientes, Alergeneos, Fotografia);

            Console.WriteLine(p1.ToString());
            Console.WriteLine(p2.ToString());

            Cliente c1 = new Cliente("António", Fotografia, "tonebiclas@gmail.com", "metejel", 913136226);
            Console.WriteLine(c1.ToString());

            

            Tuple<int, int, string> item1 = Tuple.Create(1, 1, "Com pouco arroz");
            Tuple<int, int, string> item2 = Tuple.Create(3, 2, "Com pouca massa");

            IList<Tuple<int, int, string>> items = new List<Tuple<int, int, string>>();
            items.Add(item1);
            items.Add(item2);

            Reserva r1 = new Reserva(1, items, 5.12, DateTime.Now);
            //r1.AlteraEstadoReserva(2, 'a');
           // r1.pagamento(DateTime.Now);
            //r1.AlteraEstadoReserva(2, 'e');
            Console.WriteLine(r1.ToString());


            Reserva r2 = new Reserva(1, items, 5.12, DateTime.Now);
            r2.AlteraEstadoReserva(2, 'a');
            r2.pagamento(DateTime.Now);
            r2.AlteraEstadoReserva(2, 'e');
            Console.WriteLine(r2.ToString());
        }*/
    }
}
